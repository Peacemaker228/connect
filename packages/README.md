# Packages

Эта директория зарезервирована под shared packages.

Планируемые пакеты:
- `app-core` — доменные типы, contracts, adapters
- `sdk` — typed clients для web/desktop
- `ui` — shared UI components
- `config` — shared конфигурация и tooling presets
