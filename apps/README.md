# Apps

Эта директория зарезервирована под исполняемые приложения проекта.

Планируемая структура:
- `web` — web shell
- `desktop` — desktop shell
- `api` — backend на `Nest`

На текущем этапе каркас создаётся без переноса runtime-кода.
