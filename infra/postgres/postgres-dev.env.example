# Local Postgres development only.
# This is for disposable local development data, not staging or production.

DATABASE_URL=postgresql://connect_validation:connect_validation_password@localhost:5433/connect_validation?schema=public

# Optional: keep validation tooling envs aligned with the same local service.
POSTGRES_VALIDATION_DATABASE_URL=postgresql://connect_validation:connect_validation_password@localhost:5433/connect_validation?schema=public
POSTGRES_VALIDATION_SHADOW_DATABASE_URL=postgresql://connect_validation:connect_validation_password@localhost:5433/connect_validation_shadow?schema=public
